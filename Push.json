{
    "version": 2,
    "rules": [
        {
            "domain_keyword": "ntp",
            "domain_suffix": ".push.apple.com",
            "domain_regex": "^(alt[1-8]-)?mtalk4?(-(staging|dev))?\\.google\\.com$"
        }
    ]
}